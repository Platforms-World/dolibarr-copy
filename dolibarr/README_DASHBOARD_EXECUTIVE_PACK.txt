TakePOS Executive Dashboard Pack

Approximate design aligned to the provided reference screenshot.

Included:
- takepos/dashboard.php
- takepos/js/dashboard_pack.js
- takepos/css/dashboard_pack.css
- takepos/reports/dashboard_pdf.php

Notes:
- Uses existing TakeposDashboardService data source already included in your pack.
- Uses pure CSS + inline SVG charts, no external CDN dependency.
- Keeps kafoerpcontrol / TakeposSaasBridge permission checks intact.
- PDF export remains enabled through TCPDF if available in Dolibarr.

Install:
1) Backup current files.
2) Extract over your /dolibarr/takepos/ path.
3) Clear browser cache and reload.
